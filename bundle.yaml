---
name: mlflow
applications:
  mlflow-server:
    charm: mlflow-server
    scale: 1

  mysql:
    charm: charmed-osm-mariadb-k8s
    channel: latest/stable
    scale: 1

  minio:
    charm: minio
    channel: latest/stable
    scale: 1

bundle: kubernetes
description: "MLflow"
relations:
  - - mlflow-server
    - mysql
  - - mlflow-server:object-storage
    - minio:object-storage
